// API key moved to server-side proxy (server.js) for security.
// Ani chat now calls /api/ani (local server) — key never reaches the browser.
// See .env.example to configure ANTHROPIC_API_KEY.
export const ANI_API_KEY = "";
